<?xml version="1.0" encoding="UTF-8"?>

        <?import javafx.geometry.*?>
        <?import javafx.scene.control.*?>
        <?import javafx.scene.image.*?>
        <?import javafx.scene.layout.*?>
        <?import de.jensd.fx.glyphs.fontawesome.FontAwesomeIconView?>

<VBox alignment="CENTER" prefHeight="700.0" prefWidth="1100.0" spacing="10" stylesheets="Front.css" xmlns="http://javafx.com/javafx/17.0.2-ea" xmlns:fx="http://javafx.com/fxml/1" fx:controller="cotrollers.Front">
    <children>
        <!-- Ajout d'un titre -->
        <Label styleClass="Titre" text="Explorez les rendez-vous" />
        <!-- Ajout d'un lien pour ajouter un don -->
        <Hyperlink fx:id="addRendezvous" styleClass="hyperlink" text="Ajouter rendez-vous" />
        <Button fx:id="chatbotButton" text="Chatbot" onAction="#chatbot" />
        <ScrollPane fx:id="scrollPane" fitToHeight="true" fitToWidth="true" styleClass="scroll-pane">
            <content>
                <TilePane fx:id="cardsContainer" hgap="10" prefColumns="4" styleClass="tile-pane" vgap="10">
                    <padding>
                        <Insets bottom="10" left="10" right="10" top="10" />
                    </padding>
                    <!-- Les cartes seront ajoutées ici dynamiquement -->
                    <HBox spacing="10">
                        <!-- Contenu de la carte -->
                        <VBox spacing="5">

                        </VBox>
                    </HBox>
                </TilePane>
            </content>
        </ScrollPane>
    </children>
</VBox>